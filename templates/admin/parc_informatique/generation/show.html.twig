{% extends 'base.html.twig' %}

{% block title %}Generation{% endblock %}

{% block body %}
    <h1>Generation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ generation.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ generation.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_parc_informatique_generation_index') }}">back to list</a>

    <a href="{{ path('app_admin_parc_informatique_generation_edit', {'id': generation.id}) }}">edit</a>

    {{ include('admin/parc_informatique/generation/_delete_form.html.twig') }}
{% endblock %}
