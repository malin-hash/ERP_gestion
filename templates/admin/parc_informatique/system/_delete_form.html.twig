<form method="post" action="{{ path('app_admin_parc_informatique_system_delete', {'id': system.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ system.id) }}">
    <button class="btn">Delete</button>
</form>
