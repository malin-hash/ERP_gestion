{% extends 'base.html.twig' %}

{% block title %}Liste unité centrales{% endblock %}

{% block body %}
<main id="main" class="main">


    <section class="section">
      <div class="row">
        <div class="col-lg-12">

          <div class="card">
            <div class="card-body">
              <h5 class="card-title">Liste des unité centrales</h5>
              <p>Ce tableau récapitule toutes les informations sur chaque unité centrale.</p>
                {% include "_partials/_flashmessage.html.twig" %}

              <!-- Table with stripped rows -->
              <table class="table datatable">
                <thead>
                  <tr>
                    <th scope="col">Nom de l'équipement</th>
                    <th scope="col">Type de l'équipement</th>
                    <th scope="col">Marque</th>
                    <th scope="col"><i class='ri-ball-pen-fill'></i></th>
                    <th scope="col"><i class='ri-delete-bin-2-fill'></i></th>
                  </tr>
                </thead>
                <tbody>
                {% for unitecentral in unitecentrals %}
                  <tr>
                    <td>{{ unitecentral.nom}}</td>
                    <td>{{ unitecentral.type.materiel}}</td>
                    <td>{{ unitecentral.marque.nom}}</td>
                     <td><a onclick=" return confirm('Voulez-vous Vraiment modifier cette  unite centrale 👏👏 ?')" style="color: blue;" href="{{path('app_admin_parc_informatique_unitecentral_edit', { id: unitecentral.id })}}"><i class="ri-ball-pen-line"></i></a></td>
                     <td><a onclick=" return confirm('Voulez-vous Vraiment supprimer cette unite centrale 👏👏 ?')" style="color: red;" href="{{path('app_admin_parc_informatique_unitecentral_delete', { id: unitecentral.id })}}"><i class="ri-delete-bin-6-line"></i></a></td>
              
                  </tr>
                     {% else %}
                        <tr>
                            <td colspan="7">
                             <div class="col-md-12 mx-auto mt-2">
                                 <div class="alert alert-info alert dimissible fade show">
                                     Aucune unite centrale 😎😎😎😎
                                 </div>
                             </div>
                            </td>
                        </tr>
                {% endfor %}
                </tbody>
              </table>
              <!-- End Table with stripped rows -->

            </div>
          </div>

        </div>
      </div>
    </section>

  </main><!-- End #main -->
{% endblock %}

