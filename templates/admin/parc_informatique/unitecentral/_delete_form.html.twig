<form method="post" action="{{ path('app_admin_parc_informatique_unitecentral_delete', {'id': unitecentral.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ unitecentral.id) }}">
    <button class="btn">Delete</button>
</form>
