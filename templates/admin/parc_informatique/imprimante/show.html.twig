{% extends 'base.html.twig' %}

{% block title %}Imprimante{% endblock %}

{% block body %}
    <h1>Imprimante</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ imprimante.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_parc_informatique_imprimante_index') }}">back to list</a>

    <a href="{{ path('app_admin_parc_informatique_imprimante_edit', {'id': imprimante.id}) }}">edit</a>

    {{ include('admin/parc_informatique/imprimante/_delete_form.html.twig') }}
{% endblock %}
