{% extends 'base.html.twig' %}

{% block title %}Country{% endblock %}

{% block body %}
    <h1>Country</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ country.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ country.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_clientelle_country_index') }}">back to list</a>

    <a href="{{ path('app_admin_clientelle_country_edit', {'id': country.id}) }}">edit</a>

    {{ include('admin/clientelle/country/_delete_form.html.twig') }}
{% endblock %}
