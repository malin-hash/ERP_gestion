{{ form_start(form) }}
    <main id="main" class="main">

    <div class="pagetitle">
      <h1>Formulaire Ville</h1>
      <nav>
        <ol class="breadcrumb">
          <li class="breadcrumb-item"><a href="index.html">Acceuil</a></li>
          <li class="breadcrumb-item">gestion client</li>
          <li class="breadcrumb-item active">Ville</li>
        </ol>
      </nav>
    </div><!-- End Page Title -->
    <section class="section">
      <div class="row">
        <div class="col-lg-6">

          <div class="card">
            <div class="card-body">
              <h5 class="card-title">Formulaire d'ajout d'une Ville</h5>
                {% include "_partials/_flashmessage.html.twig" %}
              <!-- Vertical Form -->
              {{ form_start(form) }}
                <div class="col-12">
                  {{ form_row(form.name, {'attr':{'class': 'form-control'}}) }}
                </div>
                <div class="col-12">
                  {{ form_row(form.country, {'attr':{'class': 'form-control'}}) }}
                </div>
                <div class="text-center">
                  <button type="submit" class="btn btn-primary">Ajouter</button>
                </div>
              {{ form_end(form) }}
            </div>
          </div>
        </div>
      </div>
    </section>

  </main><!-- End #main -->

{{ form_end(form) }}
