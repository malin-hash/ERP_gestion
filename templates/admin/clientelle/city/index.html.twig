{% extends 'base.html.twig' %}

{% block title %}Liste des Villes{% endblock %}

{% block body %}
<main id="main" class="main">


    <section class="section">
      <div class="row">
        <div class="col-lg-12">

          <div class="card">
            <div class="card-body">
              <h5 class="card-title">Liste des Villes</h5>
              <p>Ce tableau récapitule toutes les informations sur chaque Ville.</p>
                {% include "_partials/_flashmessage.html.twig" %}

              <!-- Table with stripped rows -->
              <table class="table datatable">
                <thead>
                  <tr>
                    <th scope="col">Ville</th>
                    <th scope="col">Pays</th>
                    <th scope="col"><i class='ri-ball-pen-fill'></i></th>
                    <th scope="col"><i class='ri-delete-bin-2-fill'></i></th>
                  </tr>
                </thead>
                <tbody>
                {% for city in cities %}
                  <tr>
                    <td>{{ city.name }}</td>
                    <td>{{ city.country.name }}</td>
                     <td><a onclick=" return confirm('Voulez-vous Vraiment modifier cette  Ville 👏👏 ?')" style="color: blue;" href="{{path('app_admin_clientelle_city_edit', { id: city.id })}}"><i class="ri-ball-pen-line"></i></a></td>
                     <td><a onclick=" return confirm('Voulez-vous Vraiment supprimer cette Ville 👏👏 ?')" style="color: red;" href="{{path('app_admin_clientelle_city_delete', { id: city.id })}}"><i class="ri-delete-bin-6-line"></i></a></td>
                  </tr>
                     {% else %}
                        <tr>
                            <td colspan="7">
                             <div class="col-md-12 mx-auto mt-2">
                                 <div class="alert alert-info alert dimissible fade show">
                                     Aucune Ville 😎😎😎😎
                                 </div>
                             </div>
                            </td>
                        </tr>
                {% endfor %}
                </tbody>
              </table>
              <!-- End Table with stripped rows -->

            </div>
          </div>

        </div>
      </div>
    </section>

  </main><!-- End #main -->
{% endblock %}

